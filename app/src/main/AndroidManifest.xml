<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.excq.androidopengles20demo">

    <!--告诉系统这个 APP 需要 OpenGL ES 2.0-->
    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <!--申明需要纹理压缩-->
    <supports-gl-texture android:name="GL_OES_compressed_ETC1_RGB8_texture" />
    <supports-gl-texture android:name="GL_OES_compressed_paletted_texture" />

    <!--申请网络权限-->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".WebActivity" />
        <activity
            android:name=".PrefaceActivity"
            android:label="@string/string_preface" />
        <activity android:name=".chapters1.MainActivity"
            android:label="@string/string_chapters1"/>
        <activity android:name=".chapters2.MainActivity"
            android:label="@string/string_chapters2"/>
        <activity android:name=".chapters3.MainActivity"
            android:label="@string/string_chapters3"/>
        <activity android:name=".chapters4.MainActivity"
            android:label="@string/string_chapters4"/>
        <activity android:name=".chapters5.MainActivity"
            android:label="@string/string_chapters5"/>
        <activity android:name=".chapters6.MainActivity"
            android:label="@string/string_chapters6"/>
    </application>

</manifest>